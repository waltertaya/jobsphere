import React from 'react';
import Layout from '../components/Layout';
import SeeMore from '../components/seeMore';

const SeeMoreJob: React.FC = () => {
  return (
    <Layout>
        <SeeMore />
    </Layout>
  );
};

export default SeeMoreJob;